<!--
This file starts empty. `python learning_run.py` (phase 6) REWRITES this
file from scratch each time it runs -- based on your approve/skip decisions
and interview outcomes -- e.g. "near-automatic skip: Senior Android roles
scored 75+, based on 3 skips" or "TPM roles requiring 5+ years leadership
without the exact title are converting to interviews -- weigh favorably."
It needs at least 5 decided jobs before it will write anything; until then
this file is left as-is. Every scoring run reads whatever's here and weighs
it in.

You can also write notes here yourself at any time -- but note that the
next learning_run.py run treats whatever's here as a starting point, not a
fixed rule: it may rewrite or drop anything the actual decision history no
longer supports.
-->
